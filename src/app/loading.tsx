"use client";
import MainLoader from "@/components/Unique/Loaders/MainLoader";

const Loading = () => {
  return (
    <section className="">
      <MainLoader />
    </section>
  );
};

export default Loading;
